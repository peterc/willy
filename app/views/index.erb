 	<form id="questionForm" method="post" action="/poll">
  	
  		<p>Question</p>
  		<input type="text" placeholder="What's your favorite color?" name="question" id="question" value="" required />
  		
  		<p>Choices</p>
  		<input type="text" placeholder="Red" name="answer[]" value=""  required />
  		<input type="text" placeholder="Blue" name="answer[]" value=""  required />
  		<!-- button id="remove" style="display: none;">Remove - </button -->

  		<button id="add">Add + </button>
  	
  	<button type="submit">Create Poll</button>
  	</form>

<script>
  $(document).ready(function() {

    	$('#add').click( function(e) {
    		e.preventDefault();
    		$('<input type="text" name="answer[]" value="" class="optionalAnswer"  required />').insertBefore('#add');
    		//$('#remove').show();
    	});


    	$('#remove').click( function(e) {
    		e.preventDefault;
    		$('.optionalAnswer').last().remove();
    			
    			// if last child DOES !NOT have a class of optional answer,
    			// we know we have our origional two left - so hold fire!!
    			if (!$('input[type=text]').last().hasClass('optionalAnswer')) {
    				$('#remove').hide();
    			}

    		});

    });
</script>